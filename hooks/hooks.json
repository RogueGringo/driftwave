{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|clear|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/scripts/topo.sh\" scan 2>/dev/null && bash \"${CLAUDE_PLUGIN_ROOT}/scripts/topo.sh\" validate 2>/dev/null || echo '[driftwave] topo scan deferred — run manually: ./plugins/driftwave/scripts/topo.sh figure-it-out'",
            "async": true
          }
        ]
      }
    ]
  }
}

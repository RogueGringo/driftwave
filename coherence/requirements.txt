# Core — numpy-only TF-IDF intent channel + plots
numpy>=1.24
scipy>=1.11
matplotlib>=3.7

# Optional — INTENT_MODE=neural (sentence embeddings for the intent channel)
# transformers>=4.40
# torch>=2.1

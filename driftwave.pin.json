{
  "pin_version": "1",
  "ontology": "driftwave_pin — locked invariants. The loop reads these; no agent, command, or artifact may retune them. Changing the pin is a human git commit, never a runtime act.",
  "axioms": [
    "NO_AVERAGING",
    "UPWARD_FLOW",
    "WAYPOINT_ROUTING",
    "SHAPE_OVER_COUNT",
    "ADAPTIVE_SCALE"
  ],
  "honesty_tiers": ["real", "heuristic", "planned"],
  "verdict_vocabulary": ["PASS", "FAIL", "NULL", "CERTIFIED_NULL", "NO_VERDICT"],
  "consistency_vocabulary": ["ON_SHELL", "OFF_SHELL"],
  "routing_vocabulary": ["ASCEND", "REPROBE", "SPLIT", "HOLD"],
  "flag_vocabulary": [
    "persistent_structure",
    "no_structure",
    "beats_decoy",
    "fails_decoy",
    "gate_failed",
    "schema_invalid",
    "prereg_mismatch",
    "deviation_disclosed",
    "limit_declared",
    "stage_omitted"
  ],
  "flag_vocabulary_note": "flags[] is the ONLY sanctioned significance channel on an artifact. A flag not in this list is a validation failure, not a new feature.",
  "prohibited_lexicon": [
    "proves",
    "proven",
    "guarantees",
    "guaranteed",
    "definitively",
    "infallible",
    "revolutionary",
    "breakthrough"
  ],
  "prohibited_lexicon_note": "Overclaim words may never appear in an artifact's claim-bearing fields (labels, reasons, findings). Enforced by dw_validate, not by good intentions.",
  "claim_fields": [
    "label",
    "centroid_description",
    "routing_reason",
    "verdict_reason",
    "claim",
    "incompatibility",
    "feature",
    "rule",
    "question"
  ],
  "gates": {
    "entropy_min": 0.1,
    "split_cluster_threshold": 3,
    "max_reprobe_iterations": 3,
    "eps_rule": "median-bar-lifetime"
  },
  "state": {
    "dir_resolution": "DW_STATE_DIR > $CLAUDE_PROJECT_DIR/.dw > <git toplevel>/.dw > ./.dw",
    "never": "/tmp — wiped on reboot, shared across projects; using it broke the cross-session memory claim (0.1.x)"
  }
}
